# 运行时配置；留空使用容器自带资源。修改后重启服务，无需重新构建。
# 地址对应上传包中 assets、share、vendor 三个目录的父目录。
STATIC_ASSET_BASE_URL=
# 示例：https://static.example.com/wedding/release-20260914/

LOG_FORMAT=text
LOG_LEVEL=info
LOG_SERVICE=wedding
# auto 在终端着色；日志平台可用 always；纯文本用 never。
LOG_COLOR=auto
LOG_HTTP_ASSETS=false
