# 运行时配置；留空使用容器自带资源。修改后重启服务，无需重新构建。
# 地址对应 public 内容所在的根目录；上传时保留 public 内部结构。
STATIC_ASSET_BASE_URL=
# 示例：https://static.jaryn.com.cn/

LOG_FORMAT=text
LOG_LEVEL=info
LOG_SERVICE=wedding
# auto 在终端着色；日志平台可用 always；纯文本用 never。
LOG_COLOR=auto
LOG_HTTP_ASSETS=false
